#include<stdio.h>
int main(){
	
			int a;
			int *p;
			p =& a; //address of a stored in pointer variable
			printf("%ls\n ",&a); // ls should use instead of d 
			printf("%ls\n",p);
	
	
	return 0;
}
